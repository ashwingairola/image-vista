import { MosaicGridItemDirective } from './mosaic-grid-item.directive';

describe('MosaicGridItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MosaicGridItemDirective();
    expect(directive).toBeTruthy();
  });
});
